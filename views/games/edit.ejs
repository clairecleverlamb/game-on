<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Game - Game On Sports Connect</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/games/edit.css">
    <link rel="stylesheet" href="/css/sidebar.css">

</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/sidebar') %>
    <div class="form-container">
        <h1>Edit Your Game</h1>
        <p class="form-intro">Update your game details to keep the community engaged!</p>
        <form action="/games/<%= game._id %>?_method=PUT" method="POST" class="event-form">
            <div class="form-group">
                <label for="sport">Sport:</label>
                <input type="text" id="sport" name="sport" value="<%= game.sport %>" placeholder="e.g., Volleyball" required>
            </div>
            <div class="form-group">
                <label for="location">Location:</label>
                <input type="text" id="location" name="location" value="<%= game.location %>" placeholder="e.g., Venice Beach" required>
            </div>
            <div class="form-group">
                <label for="time">Time:</label>
                <input type="datetime-local" id="time" name="time" value="<%= game.time.toISOString().slice(0,16) %>" required>
            </div>
            <div class="form-group">
                <label for="skillLevel">Skill Level:</label>
                <select id="skillLevel" name="skillLevel">
                    <option value="Beginner" <%= game.skillLevel === 'Beginner' ? 'selected' : '' %>>Beginner</option>
                    <option value="Intermediate" <%= game.skillLevel === 'Intermediate' ? 'selected' : '' %>>Intermediate</option>
                    <option value="Advanced" <%= game.skillLevel === 'Advanced' ? 'selected' : '' %>>Advanced</option>
                </select>
            </div>
            <div class="form-group">
                <label for="maxPlayers">Max Players:</label>
                <input type="number" id="maxPlayers" name="maxPlayers" value="<%= game.maxPlayers %>" min="2" placeholder="e.g., 6" required>
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="description" placeholder="Tell us about your game..." rows="4"><%= game.description || '' %></textarea>
            </div>
            <button type="submit" class="create-button">Update Game</button>
        </form>
    </div>
    <%- include('../partials/footer') %>
</body>
</html>
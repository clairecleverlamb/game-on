<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Games - Game On Sports Connect</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/subheader', { pageTitle: 'All Games', backLink: '/' }) %>
    <%- include('../partials/sidebar') %>
    <div class="index-container">
        <h1>Explore All Games</h1>
        <p class="index-intro">Find and join exciting games happening near you!</p>
        <% if (games && games.length > 0) { %>
            <div class="events-grid">
                <% games.forEach(game => { %>
                    <div class="event-card">
                        <h2><a href="/games/<%= game._id %>"><%= game.sport %></a></h2>
                        <p><strong>Location:</strong> <%= game.location %></p>
                        <p><strong>Time:</strong> <%= game.time.toLocaleString() %></p>
                        <p><strong>Players:</strong> <%= game.participants.length %> / <%= game.maxPlayers %></p>
                        <% if (game.completed) { %>
                            <span class="status ended">Ended</span>
                        <% } else if (game.participants.length >= game.maxPlayers) { %>
                            <span class="status full">Full</span>
                        <% } else { %>
                            <span class="status open">Open</span>
                        <% } %>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="no-events">No games available yet. Create one to get started!</p>
        <% } %>
        <%- include('../partials/weather', { location: games.length > 0 ? games[0].location : 'San Francisco' }) %>
    </div>
    <%- include('../partials/calendar') %>
    <%- include('../partials/footer') %>
</body>
</html>
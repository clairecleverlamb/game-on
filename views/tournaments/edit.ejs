<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Tournament - Game On Sports Connect</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/subheader', { pageTitle: 'Edit Tournament', backLink: '/tournaments/' + tournament._id }) %>
    <%- include('../partials/sidebar') %>
    <div class="form-container">
        <h1>Edit Your Tournament</h1>
        <p class="form-intro">Fine-tune your tournament details for the community!</p>
        <form action="/tournaments/<%= tournament._id %>?_method=PUT" method="POST" class="event-form">
            <div class="form-group">
                <label for="name">Tournament Name:</label>
                <input type="text" id="name" name="name" value="<%= tournament.name %>" placeholder="e.g., Spike Fest" required>
            </div>
            <div class="form-group">
                <label for="sport">Sport:</label>
                <input type="text" id="sport" name="sport" value="<%= tournament.sport %>" placeholder="e.g., Volleyball" required>
            </div>
            <div class="form-group">
                <label for="location">Location:</label>
                <input type="text" id="location" name="location" value="<%= tournament.location %>" placeholder="e.g., Central Park" required>
            </div>
            <div class="form-group">
                <label for="startDate">Start Date:</label>
                <input type="datetime-local" id="startDate" name="startDate" value="<%= tournament.startDate.toISOString().slice(0,16) %>" required>
            </div>
            <div class="form-group">
                <label for="endDate">End Date:</label>
                <input type="datetime-local" id="endDate" name="endDate" value="<%= tournament.endDate.toISOString().slice(0,16) %>" required>
            </div>
            <div class="form-group">
                <label for="bracketType">Bracket Type:</label>
                <select id="bracketType" name="bracketType">
                    <option value="Single Elimination" <%= tournament.bracketType === 'Single Elimination' ? 'selected' : '' %>>Single Elimination</option>
                    <option value="Round Robin" <%= tournament.bracketType === 'Round Robin' ? 'selected' : '' %>>Round Robin</option>
                    <option value="Group Stage" <%= tournament.bracketType === 'Group Stage' ? 'selected' : '' %>>Group Stage</option>
                </select>
            </div>
            <div class="form-group">
                <label for="teamSize">Team Size:</label>
                <input type="number" id="teamSize" name="teamSize" value="<%= tournament.teamSize %>" min="1" placeholder="e.g., 4" required>
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="description" placeholder="Details about your tournament..." rows="4"><%= tournament.description || '' %></textarea>
            </div>
            <button type="submit" class="create-button">Update Tournament</button>
        </form>
        <%- include('../partials/weather', { location: tournament.location }) %>
    </div>
    <%- include('../partials/calendar') %>
    <%- include('../partials/footer') %>
</body>
</html>
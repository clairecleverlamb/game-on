<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Tournaments - Game On Sports Connect</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/tournaments/index.css">
    <link rel="stylesheet" href="/css/sidebar.css">
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/sidebar') %>
    <div class="index-container">
        <h1>Discover All Tournaments</h1>
        <p class="index-intro">Join epic tournaments and compete with the best!</p>
        <% if (tournaments && tournaments.length > 0) { %>
            <div class="events-grid">
                <% tournaments.forEach(tournament => { %>
                    <div class="event-card">
                        <h2><a href="/tournaments/<%= tournament._id %>"><%= tournament.name %></a></h2>
                        <p><strong>Sport:</strong> <%= tournament.sport %></p>
                        <p><strong>Location:</strong> <%= tournament.location %></p>
                        <p><strong>Start:</strong> <%= tournament.startDate.toLocaleString() %></p>
                        <p><strong>Participants:</strong> <%= tournament.participants.length %> / <%= tournament.teamSize %></p>
                        <% if (tournament.completed) { %>
                            <span class="status ended">Ended</span>
                        <% } else if (tournament.participants.length >= tournament.teamSize) { %>
                            <span class="status full">Full</span>
                        <% } else { %>
                            <span class="status open">Open</span>
                        <% } %>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="no-events">No tournaments available yet. Create one to kick things off!</p>
        <% } %>
    </div>
    <%- include('../partials/footer') %>
</body>
</html>
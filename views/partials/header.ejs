<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
<nav>
    <div class="logo">
        <a href="/">Game On </a>
        <% if (weather) { %>
            <div class="weather-widget" style="display: inline-flex; align-items: center; margin-left: 1rem;">
              <img src="<%= weather.icon %>" alt="<%= weather.condition %>" style="height: 24px; margin-right: 0.3rem;">
              <span><%= weather.temp %>°F</span>
              <% if (user?.location) { %>
                <span style="margin-left: 0.5rem;">in <%= user.location %></span>
              <% } %>
            </div>
        <% } %>             

    </div>
    <div class="nav-links">
        <% if (user) { %>
            <a href="/users">Profile</a>
            <a href="/auth/sign-out">Sign Out</a>
            <% } else { %>
                <a href="/auth/sign-up">Sign Up</a>
                <a href="/auth/sign-in">Sign In</a>
                <a href="/">Browse</a>
            <% } %>
    </div>
</nav>
</body>